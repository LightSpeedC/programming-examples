'use strict';

module.exports = jsonStringifyAsync;

// jsonStringifyAsync *****************************************************************
async function jsonStringifyAsync(obj) {
	return JSON.stringify(obj);
}
