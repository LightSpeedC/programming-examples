第一級オブジェクト
第一級関数

高階関数
閉合 (closure) 含合?

a = 10;
fn = x => x * 2;
リテラル(即値)で値が作成できる

func(10);
func(fn);
func(x => x * 2);
引数に指定できる

return 10;
return fn;
return x => x * 2;
関数の戻り値として値が返せる

10 === 20
f === f2
他の値と比較できる

などの特徴がある。
